<Window x:Class="BricsCadRc.Dialogs.PunchingTagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tag piles PH" Height="240" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Source punching analysis file:"
               FontWeight="Bold" Margin="0,0,0,4"/>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBox x:Name="PathBox" Grid.Column="0" IsReadOnly="True"
               VerticalContentAlignment="Center" Padding="6,3"/>
      <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse..."
              Width="90" Margin="6,0,0,0" Click="BrowseButton_Click"/>
    </Grid>

    <TextBlock Grid.Row="3" Text="Preview:" FontWeight="Bold"
               Margin="0,0,0,4"/>
    <Border Grid.Row="4" BorderBrush="Gray" BorderThickness="1"
            Padding="8" Background="#FAFAFA">
      <TextBlock x:Name="PreviewBlock" TextWrapping="Wrap"
                 Text="Select a source file to see preview..."
                 Foreground="#666"/>
    </Border>

    <StackPanel Grid.Row="5" Orientation="Horizontal"
                HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button x:Name="OkButton" Content="OK" Width="80"
              IsDefault="True" IsEnabled="False"
              Click="OkButton_Click"/>
      <Button x:Name="CancelButton" Content="Cancel" Width="80"
              Margin="6,0,0,0" IsCancel="True"
              Click="CancelButton_Click"/>
    </StackPanel>
  </Grid>
</Window>
