<Window x:Class="BricsCadRc.Dialogs.PunchingTagResultsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RC_PUNCHING_TAG — results"
        Height="500" Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize" ShowInTaskbar="False">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0" BorderBrush="#CCC" BorderThickness="1"
            Padding="10" Background="#F8F8F8">
      <TextBlock x:Name="SummaryBlock"
                 FontFamily="Consolas" FontSize="12"
                 TextWrapping="Wrap"/>
    </Border>

    <TextBlock Grid.Row="2" x:Name="WarningsHeader"
               FontWeight="Bold" Margin="0,0,0,4"
               Text="Warnings (0):"/>

    <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1"
            Background="#FFFEF7">
      <ScrollViewer VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Auto">
        <ItemsControl x:Name="WarningsList" Padding="6">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding}"
                         FontFamily="Consolas" FontSize="11"
                         Margin="0,1,0,1"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </Border>

    <StackPanel Grid.Row="4" Orientation="Horizontal"
                HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button x:Name="CopyButton" Content="Copy to clipboard"
              Width="140" Margin="0,0,6,0"
              Click="CopyButton_Click"/>
      <Button x:Name="OkButton" Content="OK" Width="80"
              IsDefault="True" IsCancel="True"
              Click="OkButton_Click"/>
    </StackPanel>
  </Grid>
</Window>
